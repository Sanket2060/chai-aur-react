import React from 'react'

const UserContext = React.createContext()         //1.)creating a context and storing it in a 
//variable

export default UserContext;                   //2.)Export it to define to jsx elements
//It is js file only so .js is used.

