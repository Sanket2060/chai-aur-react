 Context API:
 Why to use??
 ->Avoid Prop drilling so that props shouldn't be passed to multiple components before reaching 
 the final variable.Such props stored at globally using useContext.
 Further commented on every file to understand with indexing 1,2,3,etc.